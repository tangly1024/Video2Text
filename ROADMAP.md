# Video2Text Roadmap

## 产品判断

Video2Text 的机会不在“再做一个 AI 聊天界面”，而在于成为音视频进入 AI 工作流前的
可靠转换层。目标用户是需要处理课程、访谈、会议、播客和存量视频，但不愿部署大型
平台的个人与小团队。

北极星体验：安装后用一条命令输入媒体文件，稳定得到可搜索文本、字幕和带时间戳的
结构化结果；默认可在本地完成，失败后能继续，而不是从头再跑。

## 取舍原则

1. 先支持一个本地模型后端，不同时维护多个相似方案。
2. 先 CLI 和开放文件格式，不先做 Web UI、账号、数据库或任务平台。
3. 先真实转写测试和兼容性矩阵，不追求没有业务意义的覆盖率数字。
4. 摘要、翻译、章节等生成能力只读取标准转写结果，不侵入转写核心。
5. 新依赖必须换来明显的安装简化、准确率、速度或隐私收益。

## P0：恢复可信基线（当前里程碑）

- [x] 可安装 CLI，输入路径和输出目录可配置。
- [x] 修复分段长度无效、失败片段中断合并、异常吞没和重复日志问题。
- [x] 去除未声明的 MoviePy 路径，统一由 FFmpeg/pydub 解码媒体。
- [x] 用真实业务行为测试替代“测试框架能运行”的测试。
- [x] 建立 Python 3.10–3.13 与三大桌面系统的基础 CI。
- [x] 采用 MIT 许可证，并添加 `LICENSE`。
- [ ] 用公开的短音频 fixture 做一次真实端到端烟雾测试。

完成标准：新用户能按 README 在 5 分钟内跑出结果；基础测试跨平台通过；失败有非零
退出和可理解日志。

## P1：本地优先 Whisper MVP

- 增加一个本地后端，首选验证 `faster-whisper`；CPU 默认使用小模型和量化配置，GPU
  作为可选加速，不把 CUDA 作为安装前提。
- 由模型原生时间戳生成 TXT、SRT 和 JSON，停止依赖固定 30 秒切片拼接语义。
- 缓存模型和中间结果；任务中断后从最近完成的片段继续。
- 提供 `--model`、`--device`、`--language` 三个必要参数，暂不设计复杂配置系统。
- 建立一组中文、英文、静音、噪声、长文件样例及耗时/峰值内存记录。

完成标准：普通 CPU 可离线完成 10 分钟中英文音频；输出含稳定时间戳；重复执行不会
重复计算已完成部分；敏感内容无需离开本机。

## P2：让结果直接可用

- 字幕：SRT/VTT 输出、常用换行规则、字幕与媒体时长一致性检查。
- 文稿：说话人分段先做可选实验，只有质量和安装成本可接受时才进入默认能力。
- AI 后处理：基于 JSON 转写结果提供章节、摘要、标题和待办提取；使用可替换的命令或
  API 适配层，密钥只从环境变量读取。
- 批处理：目录输入、清晰的成功/失败汇总和机器可读退出码。

完成标准：一条命令能产出可直接导入播放器的字幕，以及可供任意 LLM 使用的结构化
文本；转写与生成失败互不影响。

## 暂不做

- 自建 Web 管理后台、用户系统、数据库和云端任务队列。
- 同时接入大量云厂商，只为展示 provider 数量。
- 自研音频编解码、模型推理框架或通用工作流引擎。
- 在转写稳定前做聊天、RAG、浏览器插件和移动端。

## 建议 issue 拆分

1. `P0/license`：选择并添加许可证。
2. `P0/e2e-fixture`：加入可再分发的短音频和离线解码烟雾测试。
3. `P1/local-whisper-spike`：比较 faster-whisper 与 whisper.cpp 的安装、速度、内存和中文质量。
4. `P1/timestamp-outputs`：基于胜出后端实现 TXT/SRT/JSON。
5. `P1/resume`：定义任务清单并实现断点续跑。
6. `P2/batch`：目录批处理和汇总退出码。

## 当前 issue / PR 处置

- Issue #1、#2、#4 都指向“依赖或包无法导入”。本里程碑用可安装包、单一安装命令和
  CLI 覆盖；合并并由 CI 验证后，可分别附新版安装命令关闭。
- Issue #6 的根因是缺失片段触发 `break`，随后得到空或不完整结果。当前修复会跳过失败
  片段、保留顺序，并用回归测试锁定；全部失败时明确报错而不是静默生成空文件。
- PR #3 只补 MoviePy 依赖且 `requirements.txt` 是二进制编码。当前实现不再需要
  MoviePy，应说明替代方案后关闭，而不是继续合并旧依赖。
- PR #7 的测试诉求正确，但包含 Poetry、三项测试依赖、大量 fixture 和只验证测试框架
  的用例。当前基线用标准库 `unittest` 测真实行为；合并基线后致谢并说明已被替代。

## 技术候选依据

- [faster-whisper](https://github.com/SYSTRAN/faster-whisper)：Python 生态、本地 CPU/GPU、
  VAD、批处理和 Whisper `turbo` 模型支持，适合作为首个实现 spike。
- [whisper.cpp](https://github.com/ggml-org/whisper.cpp)：量化、CPU 与多种硬件后端覆盖好，
  适合作为低内存/跨平台候选，但集成与二进制分发成本需要实测。
- [OpenAI Whisper](https://github.com/openai/whisper)：模型与任务能力的上游基线，用于确认
  语种、时间戳和转写行为，不默认承担本项目的高性能推理层。

最终选择只看仓库样例上的安装成功率、中文质量、速度、峰值内存和分发体积，不按热度
同时接入多个后端。
